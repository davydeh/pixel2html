$(document).ready(function(){

   $('.c-features__slider').slick({
      dots: true,
      // appendDots: $('.custom-dots'),
      infinite: false,
      speed: 350,
      cssEase: 'ease-in'
   });

});
